const ageCalculate = () => {
  const today = new Date();
  const birthday = new Date(document.getElementById("birthday"));

  const birthYear = birthYear.getFullYear();
  const birthMonth = birthMonth.getMonth();
  const birthDay = birthDay.getDate();
};
